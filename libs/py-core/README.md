# @fractionax/py-core

Shared Python utilities and domain primitives, managed with [uv](https://docs.astral.sh/uv/).

```bash
moon run py-core:install   # uv sync
moon run py-core:test      # pytest
moon run py-core:lint      # ruff check
moon run py-core:typecheck # mypy
```
