# Copy to .env and fill in. Real .env files are git-ignored.
# Submodules may define their own .env.example — compose as needed.

# --- AI providers ---
ANTHROPIC_API_KEY=

# --- Shared infra (examples) ---
DATABASE_URL=
REDIS_URL=
